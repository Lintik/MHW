<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:Views="clr-namespace:MHW.Views"
             x:Class="MHW.Views.DatabasePage"
             Title="Database">
            
    <ContentPage.Content>
        <StackLayout>
            <SearchBar x:Name="SearchBar" SearchCommand="{Binding SearchCommand}" SearchCommandParameter="{Binding Text, Source={x:Reference SearchBar}}">
                <SearchBar.Behaviors>
                    <Views:TextChangedBehavior />
                </SearchBar.Behaviors>
            </SearchBar>
            
            <ListView>
                
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>